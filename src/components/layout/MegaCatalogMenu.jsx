import React from 'react';
import { Link } from 'react-router-dom';
import { ArrowRight, Sparkles } from 'lucide-react';
import { AnimatePresence, motion } from 'framer-motion';

export default function MegaCatalogMenu({ open, onEnter, onLeave, onNavigate, collections, uses, customLink }) {
  return <AnimatePresence>{open && <motion.div initial={{ opacity: 0, y: -8 }} animate={{ opacity: 1, y: 0 }} exit={{ opacity: 0, y: -8 }} transition={{ duration: 0.15 }} onMouseEnter={onEnter} onMouseLeave={onLeave} className="absolute top-full left-0 right-0 z-50 border-b border-slate-200 bg-white shadow-xl shadow-slate-900/10"><div className="max-w-7xl mx-auto px-6 lg:px-8 py-8 grid gap-9 lg:grid-cols-[1.4fr_.9fr]"><div><div className="flex items-end justify-between mb-5"><div><p className="font-mono text-[11px] tracking-[.18em] uppercase text-secondary">Kolekce MLŽIDLA®</p><p className="mt-1 text-sm text-slate-500">Vyberte podle charakteru místa.</p></div><Link to="/mlzidla-mlzitka" onClick={onNavigate} className="text-sm font-semibold text-slate-900">Celý katalog <ArrowRight size={15} className="inline"/></Link></div><div className="grid grid-cols-3 gap-4">{collections.filter((item) => item.path !== '/mlzidla-mlzitka#catalog').map((item) => <Link key={item.label} to={item.path} onClick={onNavigate} className="group"><div className="aspect-[4/3] overflow-hidden rounded-xl bg-slate-100"><img src={item.image} alt={item.label} className="w-full h-full object-cover transition-transform duration-500 group-hover:scale-105"/></div><p className="mt-3 font-heading text-sm text-slate-900">{item.label}</p><p className="mt-1 text-xs text-slate-500">{item.sub}</p></Link>)}</div><Link to={customLink.path} onClick={onNavigate} className="mt-6 flex items-center gap-3 border border-slate-200 bg-slate-50 px-4 py-3 hover:border-secondary"><Sparkles size={17} className="text-secondary"/><div><p className="text-sm font-semibold text-slate-900">{customLink.label}</p><p className="text-xs text-slate-500">{customLink.sub}</p></div></Link></div><div className="border-t border-slate-200 pt-7 lg:border-l lg:border-t-0 lg:pl-8 lg:pt-0"><p className="font-mono text-[11px] tracking-[.18em] uppercase text-secondary">B2B využití</p><p className="mt-1 text-sm text-slate-500">Řešení pro konkrétní provoz a typ projektu.</p><div className="mt-5 grid grid-cols-2 gap-1">{uses.map((item) => <Link key={item.label} to={item.path} onClick={onNavigate} className="group flex items-center gap-2 rounded-lg px-2 py-2.5 hover:bg-slate-50"><item.icon size={15} className="text-secondary"/><span className="text-sm text-slate-700 group-hover:text-slate-950">{item.label}</span></Link>)}</div></div></div></motion.div>}</AnimatePresence>;
}