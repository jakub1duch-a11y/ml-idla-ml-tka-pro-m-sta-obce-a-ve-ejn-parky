import React from 'react';
import { CartesianGrid, Line, LineChart, ResponsiveContainer, Tooltip, XAxis, YAxis } from 'recharts';

export default function WaterSavingsChart({ smartRatio, waterPrice }) {
  const data = Array.from({ length: 9 }, (_, hour) => { const standard = hour * 60; const smart = hour * 60 * smartRatio; return { hour, standard, smart, standardCost: standard * waterPrice, smartCost: smart * waterPrice }; });
  return <div className="h-72"><ResponsiveContainer width="100%" height="100%"><LineChart data={data} margin={{ top: 10, right: 20, left: -10, bottom: 15 }}><CartesianGrid stroke="#e2e8f0" vertical={false} /><XAxis dataKey="hour" label={{ value: 'Čas provozu (hodiny)', position: 'insideBottom', offset: -5 }} tickLine={false} axisLine={false} /><YAxis label={{ value: 'Celková spotřeba (litry)', angle: -90, position: 'insideLeft' }} tickLine={false} axisLine={false} domain={[0, 480]} /><Tooltip labelFormatter={(value) => `Po ${value}. hodině`} formatter={(value, key) => [`${Math.round(value)} l · ${Math.round(value * waterPrice)} Kč`, key === 'standard' ? 'Běžné spouštění' : 'Chytré řízení']} /><Line type="monotone" dataKey="standard" name="Běžné spouštění" stroke="#111827" strokeWidth={3} dot={false} /><Line type="monotone" dataKey="smart" name="Chytré řízení" stroke="#0070F3" strokeWidth={3} dot={false} /></LineChart></ResponsiveContainer></div>;
}